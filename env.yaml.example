# Copy to env.yaml, fill in, and pass with:  --env-vars-file env.yaml
# env.yaml is gitignored and gcloudignored. Do not commit it.
MONGO_DB: "test"
mongo_url: "mongodb+srv://USER:PASS@cluster0.dopn1h8.mongodb.net/"
RECOMMENDER_TOKEN: "paste-the-same-hex-string-you-give-vercel"
